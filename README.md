# Research Agent — 基于 LangGraph 的智能信息研究助手

## 项目简介
基于 LangGraph 构建的多 Agent 协作研究系统，支持"规划-研究-写作-审核"闭环工作流，具备条件回退机制。

## 示例运行

输入研究主题后,系统会自动完成 规划 → 搜索 → 写作 → 审核 四个阶段,如果审核未通过则回退重写,最多 3 次。Planner 会把主题拆解为 3-5 个子问题,下面展示 5 个子问题的运行示例。

### 1. 终端运行

```bash
$ python main.py
请输入研究主题:大语言模型的发展历程

🚀 开始研究主题: 大语言模型的发展历程

📋 Planner 正在规划主题: 大语言模型的发展历程
   生成 5 个子问题:
   • 大语言模型的起源和早期研究
   • Transformer 架构的关键突破
   • GPT 系列模型的演进
   • 开源大模型的崛起与影响
   • 当前大模型面临的挑战与未来趋势
📋 规划完成，生成 5 个子问题

🔍 Researcher 开始执行 5 个搜索:
   搜索: 大语言模型的起源和早期研究
   结果长度: 2156 字符
   搜索: Transformer 架构的关键突破
   结果长度: 1987 字符
   搜索: GPT 系列模型的演进
   结果长度: 2403 字符
   搜索: 开源大模型的崛起与影响
   结果长度: 2214 字符
   搜索: 当前大模型面临的挑战与未来趋势
   结果长度: 2371 字符
✅ 研究完成，共收集 5 条结果
🔍 研究完成，收集 5 条资料

✍️ Writer 正在撰写报告，基于 5 条研究资料...
   报告生成完成，长度: 3254 字符
✍️ 写作完成，报告长度 3254 字符

🔍 Reviewer 正在审核报告...
   评分: 9/10 | 是否通过: ✅
🔍 审核完成，评分 9/10，✅通过
✅ 审核通过，流程结束

============================================================
📄 最终研究报告
============================================================
## 执行摘要
大语言模型(Large Language Model, LLM)经历了从统计语言模型到神经网络...
...(完整报告见终端输出)
============================================================
```

**关键点**:
- 4 个阶段串行执行,每完成一步会立即打印进度
- `回退次数: N/3` 表示审核未通过时已重写的次数
- 最终报告直接打印到终端,无需打开额外文件

### 2. 示例报告输出(节选)

> 下面展示 Writer 节点生成的 Markdown 报告结构,实际内容由 LLM 基于搜索资料生成。

```markdown
## 执行摘要
大语言模型(Large Language Model, LLM)是自然语言处理领域的重大突破...
本文从起源、架构、演进、开源生态、未来趋势五个维度梳理其发展脉络。

## 核心内容
### 1. 起源与早期探索
2017 年之前,语言模型主要采用 RNN/LSTM 等序列架构 [1]...
### 2. Transformer 与注意力机制
2017 年 Google 提出的 Transformer 架构 [2] 奠定了现代 LLM 的基础...
### 3. GPT 系列的演进
从 GPT-1 到 GPT-4,参数量从 1.17 亿增长到万亿级 [3]...
### 4. 开源生态的崛起
LLaMA、Mistral、Qwen 等开源模型打破了闭源垄断 [4]...
### 5. 挑战与未来趋势
幻觉、可解释性、推理成本仍是核心难题 [5],多模态与端侧部署是主要方向...

## 结论
大语言模型的发展遵循"规模扩展 + 架构创新 + 数据多样"三条主线...
未来趋势包括多模态融合、推理能力增强和端侧部署。
```

> 引用标注 `[n]` 对应 Researcher 收集的搜索结果(每个子问题 1 条),实际编号按检索顺序生成。

## 技术栈
- **LangGraph**: 状态图编排与条件路由
- **LangChain**: LLM 调用与工具封装
- **DeepSeek API**: 大语言模型推理
- **Tavily**: 网络搜索工具

## 核心特性
1. **闭环审核**：Reviewer Agent 评分，未达标自动回退重写
2. **防死循环**：最大回退次数限制，保证流程终止
3. **状态共享**：TypedDict 定义全局状态，各 Agent 通过状态对象协作

## 快速开始
1. 配置环境：`cp .env.example .env`（填入 API Key）
2. 安装依赖：`pip install -r requirements`
3. 运行：`python main.py`

## 项目结构
```
research-agent/
├── main.py              # 入口：读取主题 → 启动工作流 → 打印报告
├── workflow.py          # LangGraph 工作流定义（节点 + 条件边）
├── state.py             # 全局状态 TypedDict
├── config.py            # 配置加载（.env + pydantic-settings）
├── agents/
│   ├── planner.py       # 规划节点：拆解主题为 3-5 个子问题
│   ├── researcher.py    # 研究节点：调用 Tavily 搜索每个子问题
│   ├── writer.py        # 写作节点：基于资料生成 Markdown 报告
│   └── reviewer.py      # 审核节点：评分 + 通过/不通过
├── tools/
│   └── search.py        # Tavily 搜索封装
├── requirements        # pip 依赖清单
├── .env.example        # 环境变量模板
└── README.md
```

## 工作流
```
START → planner → researcher → writer → reviewer
                                          │
                          ┌───────────────┼───────────────┐
                          ▼               ▼               ▼
                       (通过)        (未通过+<3次)   (未通过+≥3次)
                         END       writer(回退)          END
```

- **planner**：LLM 拆题，输出 3-5 个子问题
- **researcher**：对每个子问题调一次 Tavily 搜索，汇总结果
- **writer**：把搜索资料拼成 Markdown 报告
- **reviewer**：LLM 评分（1-10），≥8 或明确"通过：是"则放行
- **回退机制**：不通过就回 writer 重写，最多 3 次（`MAX_REVISIONS`）

## 关键设计
- **状态共享**：`ResearchState` TypedDict，所有节点通过它交换数据
- **条件路由**：`route_after_review` 三个分支，审过 / 重写 / 强制终止
- **回退上限**：`MAX_REVISIONS` 防止审核不通过时死循环
- **多模型预留**：每个 agent 可独立配置 `MODEL_*`（当前统一用 `MODEL_NAME`）